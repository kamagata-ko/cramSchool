<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cramSchool.mapper.A003Mapper">
    <insert id="insertStudent">
        insert INTO Students (
        Name
        , Email
        , Phone
        , Gender
        , address
        , dateOfBirth
        )
        VALUES (
        #{name}
        , #{email}
        , #{phone}
        , 0
        , #{address}
        , #{dateOfBirth}
        )
    </insert>
</mapper>